{"entity":"epic","version":1,"id":"runs-side-by-side","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: Two runs of one task, from any two stacks, read beside each other\n\n## Outcome\n\nA person comparing two agent runs — Claude Code against Codex, or one spec-driven plugin against another on the same harness — opens one page and reads both event streams aligned: the steps, the tool calls, the refusals, the cost, and where they diverged. Both streams came from this driver, so they are in one format regardless of what ran inside.\n\n## Why Now\n\n`beyond10x/bench` (created 2026-09-02) will hold benchmarks whose first subject is `bdfinst/agentic-dev-team` against `agentplugins`, and later harness against harness. The facts machinery exists (`aep eval run` drives `metaharness` as a tool; `aep eval matrix` counts). What does not exist is the reading surface: `metaharness project` refuses with exit 2 because `trace-ir/1` has no reader yet (`README.md` § Status at `cfcdd7a`), there is no viewer, and a third-party marketplace plugin cannot be placed into the hermetic scratch home, so the alternative stack cannot be run under this driver at all.\n\n## Scope\n\nThe `trace-ir/1` reader behind `project`; a static viewer that renders one or two streams side by side; and a `--plugin` option that installs a named marketplace plugin into the scratch config home and attests it.\n\n## Out of Scope\n\n- Scoring or ranking the two streams. The viewer aligns and shows; `aep eval matrix` counts; nobody scores.\n- Driving a harness the adapter set does not have. Pi and OpenCode are `docs/ROADMAP.md` § 2.\n- Hosting. The viewer is static HTML the bench site embeds.\n\n## Risks\n\n- The alignment rule (by state entry, by tool call index, by time) is a design choice that decides what a reader sees. Mitigation: the design page under `docs/design/` decides it before the viewer is written, per `AGENTS.md` § 7.\n- A plugin installed into the scratch home weakens the hermetic promise. Mitigation: it is opt-in, named, and the attestation lists it.\n\n## Ambiguities\n\n- `inferable` — the protocol and IR are bound by `docs/design/metaharness-protocol-v0.1.md`; a change amends that document first (`AGENTS.md:49-55`).\n- `requires-stakeholder-input` — the alignment rule. Decides: metaharness owner, on the design page. Default: align by workflow state entry when both runs are driven, else by tool-call index.\n\n## Done When\n\n`metaharness project` reads a recorded stream into `trace-ir/1`; the viewer renders the two recorded `evals/aep` runs beside each other; a run with `--plugin dev-team@bfinster` launches with the plugin present and the attestation says so.\n","owner":"metaharness","summary":"A trace-ir/1 reader behind project, a static two-column viewer, and a --plugin option that installs a named marketplace plugin into the scratch home and attests it.","tags":["bench","trace"],"title":"Two runs of one task, from any two stacks, read beside each other"},"args":{"command":"create-entity","data":{"body":"# Epic: Two runs of one task, from any two stacks, read beside each other\n\n## Outcome\n\nA person comparing two agent runs — Claude Code against Codex, or one spec-driven plugin against another on the same harness — opens one page and reads both event streams aligned: the steps, the tool calls, the refusals, the cost, and where they diverged. Both streams came from this driver, so they are in one format regardless of what ran inside.\n\n## Why Now\n\n`beyond10x/bench` (created 2026-09-02) will hold benchmarks whose first subject is `bdfinst/agentic-dev-team` against `agentplugins`, and later harness against harness. The facts machinery exists (`aep eval run` drives `metaharness` as a tool; `aep eval matrix` counts). What does not exist is the reading surface: `metaharness project` refuses with exit 2 because `trace-ir/1` has no reader yet (`README.md` § Status at `cfcdd7a`), there is no viewer, and a third-party marketplace plugin cannot be placed into the hermetic scratch home, so the alternative stack cannot be run under this driver at all.\n\n## Scope\n\nThe `trace-ir/1` reader behind `project`; a static viewer that renders one or two streams side by side; and a `--plugin` option that installs a named marketplace plugin into the scratch config home and attests it.\n\n## Out of Scope\n\n- Scoring or ranking the two streams. The viewer aligns and shows; `aep eval matrix` counts; nobody scores.\n- Driving a harness the adapter set does not have. Pi and OpenCode are `docs/ROADMAP.md` § 2.\n- Hosting. The viewer is static HTML the bench site embeds.\n\n## Risks\n\n- The alignment rule (by state entry, by tool call index, by time) is a design choice that decides what a reader sees. Mitigation: the design page under `docs/design/` decides it before the viewer is written, per `AGENTS.md` § 7.\n- A plugin installed into the scratch home weakens the hermetic promise. Mitigation: it is opt-in, named, and the attestation lists it.\n\n## Ambiguities\n\n- `inferable` — the protocol and IR are bound by `docs/design/metaharness-protocol-v0.1.md`; a change amends that document first (`AGENTS.md:49-55`).\n- `requires-stakeholder-input` — the alignment rule. Decides: metaharness owner, on the design page. Default: align by workflow state entry when both runs are driven, else by tool-call index.\n\n## Done When\n\n`metaharness project` reads a recorded stream into `trace-ir/1`; the viewer renders the two recorded `evals/aep` runs beside each other; a run with `--plugin dev-team@bfinster` launches with the plugin present and the attestation says so.\n","owner":"metaharness","status":"draft","summary":"A trace-ir/1 reader behind project, a static two-column viewer, and a --plugin option that installs a named marketplace plugin into the scratch home and attests it.","tags":["bench","trace"],"title":"Two runs of one task, from any two stacks, read beside each other"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/runs-side-by-side"},"payload":{"actor":"human:timo","at":1788383311346,"causation":"cmd-new-epic-runs-side-by-side","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:runs-side-by-side@1#0~ed7a4422bbfabb29","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"own-planning-store","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: metaharness plans in a store of its own\n\n## Outcome\n\nAnybody working on metaharness finds its plan in `.engineering/planning/`, mutated only through `aep artifact`, pinned to a protocol tree by commit.\n\n## Context\n\nUntil 2026-09-02 metaharness had no `.engineering/` directory. `docs/ROADMAP.md` carries operator-scheduled directions and stays; the store holds the work items derived from them and from `beyond10x/bench`.\n\n## Acceptance\n\n- `.engineering/project.yaml` names the `aep` tree by a 40-hex commit and `development.standard`.\n- `aep artifact validate` exits 0.\n- `AGENTS.md` names the store and the rule that a roadmap item becomes an epic here before code.\n\n## Out of Scope\n\nConverting every roadmap section into an epic. Only the ones with work behind them.\n\n## Ambiguities\n\n- `inferable` — the pin is `a054945cf55229861b7e7b9e83e94343278cbc02`, `aep` tag `0.42.0`.\n\n## Open Questions\n\nNone.\n","owner":"metaharness","summary":"The .engineering store, pinned to aep 0.42.0; roadmap items become epics here before code.","tags":["store"],"title":"metaharness plans in a store of its own"},"args":{"command":"create-entity","data":{"body":"# Story: metaharness plans in a store of its own\n\n## Outcome\n\nAnybody working on metaharness finds its plan in `.engineering/planning/`, mutated only through `aep artifact`, pinned to a protocol tree by commit.\n\n## Context\n\nUntil 2026-09-02 metaharness had no `.engineering/` directory. `docs/ROADMAP.md` carries operator-scheduled directions and stays; the store holds the work items derived from them and from `beyond10x/bench`.\n\n## Acceptance\n\n- `.engineering/project.yaml` names the `aep` tree by a 40-hex commit and `development.standard`.\n- `aep artifact validate` exits 0.\n- `AGENTS.md` names the store and the rule that a roadmap item becomes an epic here before code.\n\n## Out of Scope\n\nConverting every roadmap section into an epic. Only the ones with work behind them.\n\n## Ambiguities\n\n- `inferable` — the pin is `a054945cf55229861b7e7b9e83e94343278cbc02`, `aep` tag `0.42.0`.\n\n## Open Questions\n\nNone.\n","owner":"metaharness","status":"draft","summary":"The .engineering store, pinned to aep 0.42.0; roadmap items become epics here before code.","tags":["store"],"title":"metaharness plans in a store of its own"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/own-planning-store"},"payload":{"actor":"human:timo","at":1788383311488,"causation":"cmd-new-story-own-planning-store","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:own-planning-store@1#0~236e524d4adbe7be","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"own-planning-store","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:runs-side-by-side"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000004","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383311497,"causation":"cmd-story-own-planning-store-decomposes-epic-runs-side-by-side","change":{"change":"related","relation":"decomposes","target":"epic:runs-side-by-side"},"correlation":"protocol-artifact-relate","event_id":"story:own-planning-store@1#0~3b2985be0bb70b96","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"trace-ir-reader","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: `metaharness project` reads an event stream into `trace-ir/1`\n\n## Outcome\n\nA recorded run becomes a content-addressed `trace-ir/1` document that `aep trace check` and the viewer both read, so one recording serves judging and reading.\n\n## Context\n\n`README.md` § Status: \"`project` | refuses with exit 2 — `trace-ir/1` has no reader yet\". The IR is defined on the `aep` side (`crates/trace-domain/src/ir.rs`); `aep trace inspect` already reports events, tool traffic and per-step timings from a transcript. This story gives the driver's own event stream the same destination.\n\n## Acceptance\n\n- `metaharness project <events.jsonl>` writes a `trace-ir/1` document; the same input yields the same bytes (no clock, no network).\n- Every event kind the protocol emits maps to an IR node or is listed in the document as `unk` with its kind, never dropped silently.\n- `aep trace check` over the projected document judges the two recorded `evals/aep` runs with no `unk` rows for the kinds the driver emits.\n- The design document is amended where the mapping is a decision.\n\n## Out of Scope\n\nProjecting a foreign transcript (`audit` over one still refuses); a Claude Code JSONL that did not pass through this driver is not this story.\n\n## Ambiguities\n\n- `inferable` — the IR shape: `aep/crates/trace-domain/src/ir.rs` at `a054945`.\n\n## Open Questions\n\nNone.\n","owner":"metaharness","summary":"The reader the README says is missing: byte-stable projection, every event kind mapped or listed as unk, judged by aep trace check.","tags":["trace"],"title":"metaharness project reads an event stream into trace-ir/1"},"args":{"command":"create-entity","data":{"body":"# Story: `metaharness project` reads an event stream into `trace-ir/1`\n\n## Outcome\n\nA recorded run becomes a content-addressed `trace-ir/1` document that `aep trace check` and the viewer both read, so one recording serves judging and reading.\n\n## Context\n\n`README.md` § Status: \"`project` | refuses with exit 2 — `trace-ir/1` has no reader yet\". The IR is defined on the `aep` side (`crates/trace-domain/src/ir.rs`); `aep trace inspect` already reports events, tool traffic and per-step timings from a transcript. This story gives the driver's own event stream the same destination.\n\n## Acceptance\n\n- `metaharness project <events.jsonl>` writes a `trace-ir/1` document; the same input yields the same bytes (no clock, no network).\n- Every event kind the protocol emits maps to an IR node or is listed in the document as `unk` with its kind, never dropped silently.\n- `aep trace check` over the projected document judges the two recorded `evals/aep` runs with no `unk` rows for the kinds the driver emits.\n- The design document is amended where the mapping is a decision.\n\n## Out of Scope\n\nProjecting a foreign transcript (`audit` over one still refuses); a Claude Code JSONL that did not pass through this driver is not this story.\n\n## Ambiguities\n\n- `inferable` — the IR shape: `aep/crates/trace-domain/src/ir.rs` at `a054945`.\n\n## Open Questions\n\nNone.\n","owner":"metaharness","status":"draft","summary":"The reader the README says is missing: byte-stable projection, every event kind mapped or listed as unk, judged by aep trace check.","tags":["trace"],"title":"metaharness project reads an event stream into trace-ir/1"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/trace-ir-reader"},"payload":{"actor":"human:timo","at":1788383311628,"causation":"cmd-new-story-trace-ir-reader","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:trace-ir-reader@1#0~cf9a458cbec6b7f2","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"trace-ir-reader","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:runs-side-by-side"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000009","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383311635,"causation":"cmd-story-trace-ir-reader-decomposes-epic-runs-side-by-side","change":{"change":"related","relation":"decomposes","target":"epic:runs-side-by-side"},"correlation":"protocol-artifact-relate","event_id":"story:trace-ir-reader@1#0~5bc0b3c34d6edd43","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"event-stream-viewer","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: One page renders two `trace-ir/1` streams side by side\n\n## Outcome\n\nA reader opens one static page with two runs and sees them aligned — state by state, call by call — with refusals, timings and cost per column, and a marker where the two diverge.\n\n## Context\n\nThe bench's first deliverable is *both stacks on one corpus*, and the operator asked to visualise the event stream side by side (2026-09-02). The viewer reads `trace-ir/1` only, so a run from any adapter renders the same.\n\n## Acceptance\n\n- A static HTML page (no server) loads one or two `trace-ir/1` documents and renders columns: state entries, tool calls with decision, refusals, per-step duration, cumulative cost.\n- Alignment follows the design page's rule; a step present in one run and absent in the other is shown as a gap, not skipped.\n- The page is generated by `metaharness project --html` or a sibling verb, and the bench site embeds it.\n- The two recorded `evals/aep` runs render as the fixture, checked in `task docs:build`.\n\n## Out of Scope\n\nLive rendering of a running session; interactivity beyond expand/collapse.\n\n## Ambiguities\n\n- `requires-stakeholder-input` — the alignment rule (see the epic). Decides: metaharness owner.\n\n## Open Questions\n\nNone.\n","owner":"metaharness","summary":"Static HTML, two aligned columns of states, calls, refusals, timings and cost; generated from the IR; embedded by the bench site.","tags":["trace","viewer"],"title":"One page renders two trace-ir/1 streams side by side"},"args":{"command":"create-entity","data":{"body":"# Story: One page renders two `trace-ir/1` streams side by side\n\n## Outcome\n\nA reader opens one static page with two runs and sees them aligned — state by state, call by call — with refusals, timings and cost per column, and a marker where the two diverge.\n\n## Context\n\nThe bench's first deliverable is *both stacks on one corpus*, and the operator asked to visualise the event stream side by side (2026-09-02). The viewer reads `trace-ir/1` only, so a run from any adapter renders the same.\n\n## Acceptance\n\n- A static HTML page (no server) loads one or two `trace-ir/1` documents and renders columns: state entries, tool calls with decision, refusals, per-step duration, cumulative cost.\n- Alignment follows the design page's rule; a step present in one run and absent in the other is shown as a gap, not skipped.\n- The page is generated by `metaharness project --html` or a sibling verb, and the bench site embeds it.\n- The two recorded `evals/aep` runs render as the fixture, checked in `task docs:build`.\n\n## Out of Scope\n\nLive rendering of a running session; interactivity beyond expand/collapse.\n\n## Ambiguities\n\n- `requires-stakeholder-input` — the alignment rule (see the epic). Decides: metaharness owner.\n\n## Open Questions\n\nNone.\n","owner":"metaharness","status":"draft","summary":"Static HTML, two aligned columns of states, calls, refusals, timings and cost; generated from the IR; embedded by the bench site.","tags":["trace","viewer"],"title":"One page renders two trace-ir/1 streams side by side"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/event-stream-viewer"},"payload":{"actor":"human:timo","at":1788383311771,"causation":"cmd-new-story-event-stream-viewer","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:event-stream-viewer@1#0~febcf9820b1ceeae","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"event-stream-viewer","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:runs-side-by-side"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000014","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383311779,"causation":"cmd-story-event-stream-viewer-decomposes-epic-runs-side-by-side","change":{"change":"related","relation":"decomposes","target":"epic:runs-side-by-side"},"correlation":"protocol-artifact-relate","event_id":"story:event-stream-viewer@1#0~ab86b8454367a56d","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"event-stream-viewer","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:runs-side-by-side"},{"depends_on":"story:trace-ir-reader"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000014","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788383311789,"causation":"cmd-story-event-stream-viewer-depends_on-story-trace-ir-reader","change":{"change":"related","relation":"depends_on","target":"story:trace-ir-reader"},"correlation":"protocol-artifact-relate","event_id":"story:event-stream-viewer@1#0~21f409acfa909e7c","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"vendor-plugin-in-scratch-home","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: A marketplace plugin is installed into the hermetic scratch home, on request and attested\n\n## Outcome\n\nA third-party Claude Code plugin — the first is `dev-team@bfinster` — runs under this driver with the same event stream as ours, so the bench compares two stacks through one instrument.\n\n## Context\n\nHermeticity means \"no ambient plugins\" (`README.md` § The three promises). The bench needs the opposite on purpose: a named plugin placed into the scratch config home before launch. The `plugin` arm of `aep eval run` presumes the shipped plugin is installed; how it gets into the scratch home is not stated in `aep eval run --help` at 0.42.0.\n\n## Acceptance\n\n- `metaharness run claude --plugin <marketplace-repo>@<name>[@<version>]` installs the plugin into the scratch home before launch, pinned to a commit or version, and refuses an unpinned one.\n- The hermetic attestation lists every installed plugin; a run with none says `plugins: none`.\n- A conformance vector records the launch with a plugin present.\n- `aep eval run --arm plugin` can pass the option through (the `aep` side is a one-line change filed there when this lands).\n\n## Out of Scope\n\nCodex plugins; Pi and OpenCode.\n\n## Ambiguities\n\n- `inferable` — Claude Code's marketplace install form is `/plugin marketplace add <repo>` then `/plugin install <name>@<marketplace>`; the headless equivalent is to be established by a research note under `docs/research/` first (`AGENTS.md` § research).\n\n## Open Questions\n\nNone.\n","owner":"metaharness","summary":"metaharness run claude --plugin <repo>@<name>@<pin> places a third-party plugin into the scratch home; the attestation lists it.","tags":["bench","hermetic"],"title":"A marketplace plugin is installed into the hermetic scratch home, on request and attested"},"args":{"command":"create-entity","data":{"body":"# Story: A marketplace plugin is installed into the hermetic scratch home, on request and attested\n\n## Outcome\n\nA third-party Claude Code plugin — the first is `dev-team@bfinster` — runs under this driver with the same event stream as ours, so the bench compares two stacks through one instrument.\n\n## Context\n\nHermeticity means \"no ambient plugins\" (`README.md` § The three promises). The bench needs the opposite on purpose: a named plugin placed into the scratch config home before launch. The `plugin` arm of `aep eval run` presumes the shipped plugin is installed; how it gets into the scratch home is not stated in `aep eval run --help` at 0.42.0.\n\n## Acceptance\n\n- `metaharness run claude --plugin <marketplace-repo>@<name>[@<version>]` installs the plugin into the scratch home before launch, pinned to a commit or version, and refuses an unpinned one.\n- The hermetic attestation lists every installed plugin; a run with none says `plugins: none`.\n- A conformance vector records the launch with a plugin present.\n- `aep eval run --arm plugin` can pass the option through (the `aep` side is a one-line change filed there when this lands).\n\n## Out of Scope\n\nCodex plugins; Pi and OpenCode.\n\n## Ambiguities\n\n- `inferable` — Claude Code's marketplace install form is `/plugin marketplace add <repo>` then `/plugin install <name>@<marketplace>`; the headless equivalent is to be established by a research note under `docs/research/` first (`AGENTS.md` § research).\n\n## Open Questions\n\nNone.\n","owner":"metaharness","status":"draft","summary":"metaharness run claude --plugin <repo>@<name>@<pin> places a third-party plugin into the scratch home; the attestation lists it.","tags":["bench","hermetic"],"title":"A marketplace plugin is installed into the hermetic scratch home, on request and attested"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/vendor-plugin-in-scratch-home"},"payload":{"actor":"human:timo","at":1788383311923,"causation":"cmd-new-story-vendor-plugin-in-scratch-home","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:vendor-plugin-in-scratch-home@1#0~fca817d62277fb8d","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
{"entity":"story","version":1,"id":"vendor-plugin-in-scratch-home","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:runs-side-by-side"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000021","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788383311935,"causation":"cmd-story-vendor-plugin-in-scratch-home-decomposes-epic-runs-side-by-side","change":{"change":"related","relation":"decomposes","target":"epic:runs-side-by-side"},"correlation":"protocol-artifact-relate","event_id":"story:vendor-plugin-in-scratch-home@1#0~b78015c0d16ce2b1","executor":null,"recorded_at":"2026-09-02T21:08:31Z"}}
